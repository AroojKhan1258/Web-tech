<%- include("../partials/header") %>

<div class="container py-5">
  <h2>Admin Dashboard</h2>

  <a href="/admin/add" class="btn mb-3">+ Add Product</a>

  <table class="table table-dark table-bordered">
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Category</th>
      <th>Actions</th>
    </tr>

    <% products.forEach(p => { %>
      <tr>
        <td><%= p.name %></td>
        <td>₨ <%= p.price %></td>
        <td><%= p.category %></td>
        <td>
          <a href="/admin/edit/<%= p._id %>" class="btn btn-warning btn-sm">Edit</a>
          <a href="/admin/delete/<%= p._id %>" class="btn btn-danger btn-sm">Delete</a>
        </td>
      </tr>
    <% }) %>
  </table>
</div>

<%- include("../partials/footer") %>
